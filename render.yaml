services:
  - type: web
    name: boa-contacts
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn step2_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
